# Copy to /etc/annotation-pipeline.env and fill with VM-specific values.
# Do not commit real endpoints or secrets.

OPENCLAW_ENDPOINT=https://openclaw-endpoint.example.com
OPENCLAW_API_KEY=replace-me
